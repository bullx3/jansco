$('#ranks_ranking_0').html(
	"<%= escape_javascript(render 'ajax_ranks_ranking', gamekind: 0) %>"
);
$('#ranks_ranking_1').html(
	"<%= escape_javascript(render 'ajax_ranks_ranking', gamekind: 1) %>"
);
$('#ranks_triger').html(
	"<%= escape_javascript('拡張ランキングを表示しました') %>"
);

